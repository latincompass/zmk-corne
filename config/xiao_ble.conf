# zmk-corne-main/config/xiao_ble.conf
# 强制启用 ZMK 板卡兼容模式（新版 xiao_ble 必需）
CONFIG_ZMK_BOARD_COMPAT=y

# 保留 XIAO BLE 必需的蓝牙配置
CONFIG_BT=y
CONFIG_BT_CTLR=y
CONFIG_BT_CTLR_SYNC_PERIODIC_MAX=16

# 可选：确保 ZMK 核心功能启用
CONFIG_ZMK=y
CONFIG_ZMK_KEYBOARD=y
CONFIG_ZMK_BLE=y
